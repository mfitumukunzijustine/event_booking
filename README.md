# event_booking
The Event Booking API is a backend application that allows users to browse events, view available seats, and make reservations. It ensures that events cannot be overbooked and handles user bookings reliably using database transactions.
